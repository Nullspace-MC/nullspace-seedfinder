USAGE:
  find_origin_structures [OPTION]...
    --help    (-h)
    --start_seed=<integer>
        (Defaults to 0)
    --end_seed=<integer>
        (Defaults to 281474976710656)
    --work_unit=<integer>
        (Optional, specifies a work unit 0-255)
    --num_units=<integer>
        (Defaults to 1)
    --quadhut_list=<file path>
        (Defaults to ./seeds/quadhut_list.txt)
    --num_threads=<integer>
        (Defaults to 1)
